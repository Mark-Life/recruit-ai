{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bun x ultracite fix >/dev/null 2>&1 || true"
          },
          {
            "type": "command",
            "command": "output=$(bun x ultracite check 2>&1); if [ $? -ne 0 ]; then echo \"$output\"; exit 2; fi"
          },
          {
            "type": "command",
            "command": "output=$(bun turbo typecheck 2>&1); if [ $? -ne 0 ]; then echo \"$output\"; exit 2; fi"
          }
        ]
      }
    ]
  }
}
